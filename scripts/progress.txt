## Codebase Patterns
- Spring `${VAR:default}` syntax provides env var fallbacks in `application.properties` — no extra config needed
- Backend `.gitignore` is at `backend-java/portfolio/.gitignore` — add `.env` there for secret file exclusion
---

# Ralph Progress Log
Started: Tue Feb 24 16:22:01 PST 2026
---

## 2026-02-24 - US-001
- What was implemented: Added fallback defaults to `application.properties` for all three required env vars using Spring's `${VAR:default}` syntax. JWT secret fallback is 41 chars (meets HMAC-SHA256 minimum of 32).
- Files changed: `backend-java/portfolio/src/main/resources/application.properties`
- **Learnings for future iterations:**
  - Spring `${VAR:default}` is all that's needed — no profiles or extra dependencies required
  - JWT secret fallback must be ≥32 characters to satisfy `Keys.hmacShaKeyFor()` minimum key length
---

## 2026-02-24 - US-002
- What was implemented: Created `.env.example` documenting required env vars for production. Added `.env` to backend `.gitignore`.
- Files changed: `backend-java/portfolio/.env.example` (new), `backend-java/portfolio/.gitignore`
- **Learnings for future iterations:**
  - `.env.example` is committed to the repo but the actual `.env` file is excluded via `.gitignore`
  - The backend `.gitignore` did not previously exclude `.env` files — added `### Environment secrets ### .env` section
---
