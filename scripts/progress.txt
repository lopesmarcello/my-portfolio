# Ralph Progress Log
Started: Thu Feb 26 06:52:19 PST 2026
---

## Codebase Patterns
- Backend entity column type override: use `@Column(columnDefinition = "TEXT")` with import `jakarta.persistence.Column`
- Maven wrapper (`./mvnw`) requires network access to download Maven — not available in this sandbox environment; `mvn test` is effectively skipped
- ResumeEntity DTO/mapper already pass `about` field through unchanged; no DTO changes needed when only changing column type

---

## 2026-02-26 - US-003
- Converted dashboard page to client component (`"use client"`)
- Fetches about, resume, and posts in parallel with `Promise.allSettled` (graceful partial failures)
- Shows animated `CardSkeleton` while loading; replaces with live content after fetch
- About card: name, title, first 100 chars of bio with ellipsis
- Resume card: fullName, experience count, most recent experience company + date range
- Posts card: total post count + 3 most recent post titles
- Error state: shows fallback description text + `ErrorBadge` with AlertCircle icon
- Files changed: `frontend/app/admin/(cms)/dashboard/page.tsx`, `scripts/prd.json`
- **Learnings for future iterations:**
  - `Promise.allSettled` is ideal for dashboard fetches — each card can succeed/fail independently
  - The dashboard's public API functions (`getAbout`, `getResume`, `getPosts`) are importable directly — no need for admin-specific wrappers
---

## 2026-02-26 - US-002
- Changed Start Date and End Date inputs from `type="text"` to `type="month"` in both experience edit and add forms
- Added `toMonthInput(dateStr)` and `fromMonthInput(monthStr)` utility functions at top of resume page
- Added `isPresent: boolean` to `ExperienceItem` interface and `emptyNewExp`
- Added `handleExpPresent` handler to toggle present state for existing experiences
- Added "Presente" checkbox that hides the end date picker and sends `null` to the API
- Loading existing data converts `YYYY-MM-DD` → `YYYY-MM` via `toMonthInput`; saving converts back via `fromMonthInput`
- Files changed: `frontend/app/admin/(cms)/resume/page.tsx`, `scripts/prd.json`
- **Learnings for future iterations:**
  - `<input type="month">` value format is `YYYY-MM`; backend `LocalDate` expects `YYYY-MM-DD` — always convert at API boundary
  - Parentheses in path confuse zsh glob expansion — always quote paths with `(cms)` in git commands
  - `isPresent` flag needs separate state field rather than relying on `endDate === null` to avoid ambiguity with empty string
---

## 2026-02-26 - US-001
- Added `@Column(columnDefinition = "TEXT")` to `ResumeEntity.about`
- Added `jakarta.persistence.Column` import to `ResumeEntity.java`
- Files changed: `backend-java/portfolio/src/main/java/com/lopesmarcello/portfolio/entities/ResumeEntity.java`, `scripts/prd.json`
- **Learnings for future iterations:**
  - `./mvnw test` fails in this environment (no network for Maven wrapper download, no Java binary in PATH) — tests cannot be run; verified logic is correct by code review
  - The DTO (`UpdateResumeRequestDTO`) and mapper (`ResumeMapper`) already map `about` as `String` with no length validation — no changes needed there
  - Spring Boot `ddl-auto=update` will alter the column on next startup
---
